- This is the folder where currently, the Rainbow (Unicorn) data is serialized to
- The serialized data is included in the solution, but not visible, so find this folder on the filesystem to see what's going on
- You can change which folder to find serialized data in, by changing the AdditionalFiles in the project file